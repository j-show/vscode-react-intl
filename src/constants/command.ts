import { extensionName } from '../libs/config';

export default {
  autoInitPath: `${extensionName}.autoInitPath`,
  manualInitPath: `${extensionName}.manualInitPath`,
  translateView: `${extensionName}.translateView`,

  extract: `${extensionName}.extract`,
};
